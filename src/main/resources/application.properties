spring.application.name=renovation

# DATABASE
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/renovation_db}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Admin@123}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT
app.jwt.secret=${JWT_SECRET:renovation-project-secret-key-must-be-at-least-32-characters-long}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

# DEFAULT ADMIN
app.admin.username=${ADMIN_USERNAME:admin}
app.admin.password=${ADMIN_PASSWORD:Admin@123}
app.admin.email=${ADMIN_EMAIL:choezom988@gmail.com}
app.admin.full-name=${ADMIN_FULL_NAME:Karma Choezom}